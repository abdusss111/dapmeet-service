# Используем официальный легковесный образ Redis
FROM redis:alpine

# Копируем кастомный конфиг, если он понадобится в будущем
# COPY redis.conf /usr/local/etc/redis/redis.conf

# Указываем Redis использовать наш конфиг при запуске
# CMD [ "redis-server", "/usr/local/etc/redis/redis.conf" ]

# Если кастомный конфиг не нужен, можно оставить файл пустым
# или просто использовать базовый образ без Dockerfile.
# Этот файл создан для возможности кастомизации в будущем.
